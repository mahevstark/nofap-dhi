import '../../../core/app_export.dart';/// This class is used in the [exercisecomponentlist_item_widget] screen.
class ExercisecomponentlistItemModel {ExercisecomponentlistItemModel({this.exerciseText, this.skipText, this.id, }) { exerciseText = exerciseText  ?? "Do 10 push-ups";skipText = skipText  ?? "Skip>>";id = id  ?? ""; }

String? exerciseText;

String? skipText;

String? id;

 }
